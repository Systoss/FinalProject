<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FinalProject.App"
             x:Class="FinalProject.App.MainPage"
             BindingContext="{Binding Main,Source={StaticResource Locator}}">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="Entrer une adresse"></Label>
        <Entry Text="{Binding SelectedAdresse}"></Entry>
        <Button Text="Search" Command="{Binding GetCoordonneCommand}"></Button>
        <Label Text="{Binding Coordonnee}"></Label>
    </StackLayout>

</ContentPage>
